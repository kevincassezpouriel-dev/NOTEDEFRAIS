import Dashboard from "@/components/Dashboard";

export const dynamic = "force-dynamic";

/** Statistiques d'acquisition détaillées (scans, appareils, villes, sources). */
export default function AcquisitionPage() {
  return <Dashboard />;
}
